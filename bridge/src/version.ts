/**
 * Bridge package version.
 *
 * FOR-DEV: source this from package.json at build time instead of hardcoding
 * (src/version.ts). Keep in sync with bridge/package.json until then.
 */
export const BRIDGE_VERSION = '0.1.0';
