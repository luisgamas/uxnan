/// Asset paths for the bundled coding-agent logos (`assets/images/agents/`).
class AgentLogos {
  const AgentLogos._();

  static const String _base = 'assets/images/agents';

  /// Claude Code logo (orange).
  static const String claude = '$_base/claudecode.svg';

  /// Gemini logo (blue).
  static const String gemini = '$_base/gemini.svg';

  /// Codex logo (white).
  static const String codex = '$_base/codex.svg';

  /// Kimi logo (blue).
  static const String kimi = '$_base/kimi-color.svg';

  /// Antigravity logo (multicolor).
  static const String antigravity = '$_base/antigravity.svg';

  /// Gemma logo (gradient).
  static const String gemma = '$_base/gemma.svg';

  /// Qwen logo (gradient).
  static const String qwen = '$_base/qwen.svg';

  /// Grok logo (monochrome).
  static const String grok = '$_base/grok.svg';

  /// OpenCode logo (monochrome).
  static const String opencode = '$_base/opencode.svg';

  /// Goose logo (monochrome).
  static const String goose = '$_base/goose.svg';

  /// Kilo Code logo (monochrome).
  static const String kilocode = '$_base/kilocode.svg';
}
