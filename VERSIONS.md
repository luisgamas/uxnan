# Release versions

Tracks which version of each Uxnan component shipped, and when. Components
version **independently** (each has its own patch), but a shared
`-alpha.YYYYMMDD` date suffix marks releases cut on the same day, so you can tell
which versions "go together".

## Convention

- Base SemVer starts at `0.0.1` (pre-1.0 = unstable; breaking changes allowed).
- Alpha builds use `0.0.PATCH-alpha.YYYYMMDD`. The `YYYYMMDD` date orders
  correctly under SemVer/npm.
- **Desktop channels are intentionally different.** The tag is the channel's
  source of truth, not a manual GitHub Release checkbox:
  - **Stable:** `desktop-stable-v0.0.PATCH` (for example,
    `desktop-stable-v0.0.10`). It produces a normal GitHub Release and feeds the
    stable updater manifest.
  - **Nightly:** `desktop-nightly-v0.0.PATCH-nightly.YYYYMMDD.N` (for example,
    `desktop-nightly-v0.0.11-nightly.20260712.1`). `N` starts at `1` and only
    distinguishes multiple nightlies cut on the same date. It produces a GitHub
    pre-release and feeds the nightly updater manifest.
  - The numeric `0.0.PATCH` base must be **new for every Desktop build in either
    channel**. Windows MSI and Tauri's updater compare only that numeric base, so
    reusing it would make a newer nightly invisible. Choose a base greater than
    every already-shipped Desktop build; switching from a higher nightly build to
    an older stable build is a downgrade and is intentionally not automatic.
- Per-component git tags drive releases:
  `shared-v*`, `bridge-v*`, `relay-v*`, `desktop-stable-v*`,
  `desktop-nightly-v*`, `mobile-v*`
  (mobile may append `+<buildNumber>`, e.g. `mobile-v0.0.1-alpha.20260621+5`).
- **`web/` is deliberately not in any of this.** The marketing site publishes no
  artifact and has no consumers, so it carries no tag and no row in the history
  below. A push to `main` runs `deploy-web.yml`, which builds it on GitHub's
  runners and uploads the static export to Cloudflare Pages. See
  [`web/docs/deploy.md`](web/docs/deploy.md).
- **Source tracks the tag — bump EVERY version file AND its lockfile in the same
  commit.** A stale lockfile is silent drift: the npm/desktop release workflows
  re-apply the version at build time with `--allow-same-version`, which **masks**
  an un-bumped source lock (that is exactly how `uxnandesktop/package-lock.json`
  sat at `0.0.2` while the app shipped `0.0.3`/`0.0.4`). So bump **all** of a
  component's version-bearing files, and re-sync the lockfile, before tagging:
  - **npm (shared / bridge / relay):** `package.json` **and the root
    `package-lock.json`** — use `npm version <v> -w <ws> --no-git-tag-version`
    (it updates **both**), not a hand edit of `package.json`.
  - **desktop:** the **numeric base** (`0.0.PATCH`, MSI-safe — the Windows MSI
    rejects a non-numeric pre-release id; the full version rides the tag + the
    compiled-in `UXNAN_VERSION`) in **all five**: `src-tauri/tauri.conf.json`,
    `src-tauri/Cargo.toml`, **`src-tauri/Cargo.lock`** (the `uxnan-desktop`
    entry), `uxnandesktop/package.json`, **and `uxnandesktop/package-lock.json`**
    (`npm install --package-lock-only` to re-sync the lock). Do **not** rely on
    the CI `npm version` step to fix the lock — that leaves the committed lock
    drifting.
  - **mobile:** `pubspec.yaml` (its `pubspec.lock` carries no app version).
    `release-mobile.yml` **fails** the release on a pubspec↔tag mismatch.
  - **Verify before tagging:** each manifest version **equals** its lockfile
    counterpart (`node -p "require('./uxnandesktop/package-lock.json').version"`
    etc.). Never commit a manifest/lock version mismatch.
- **Push `shared-v*` FIRST and let it finish before `bridge-v*`/`relay-v*`.**
  `release-npm.yml` pins the dependency by reading `npm view @uxnan/shared
  dist-tags.latest` **at build time**, so two tags pushed together race: the
  bridge job resolves the *previous* shared and publishes against it. That is a
  real break, not cosmetic — `HandlerRouter` validates every request against
  `isKnownMethod` from the shared registry, so a bridge pinned to an older
  shared answers this cycle's new methods with "method not found". It happened
  on 2026-08-03 (`bridge-v0.0.14-alpha.20260803`, superseded by `0.0.15`).
  Wait for the shared run to go green and for `npm view @uxnan/shared version`
  to report the new version before tagging the bridge.
- npm packages publish to the **`latest`** dist-tag, so `npm install`
  (`npm install -g uxnan-bridge`) and the bridge's self-update check always
  resolve the **newest** release. Pre-release channels (`alpha`/`beta`) are
  **opt-in** — the maintainer adds them manually per build when wanted, e.g.
  `npm dist-tag add uxnan-bridge@<version> beta`. (Historically the workflow
  published under `alpha`, which left `latest` stuck at the very first version;
  see *Fixing an already-published package's `latest`* below.)
- Mobile ships to **Google Play** (open testing / beta); desktop to **GitHub
  Releases** (draft).

## Release checklist

> **A workflow does all of this now.** Actions → **Release — cut versions**
> computes the versions, writes the files, commits, tags and pushes in the right
> order (waiting for npm between `shared` and its consumers), and the desktop
> nightly cuts itself at 06:20 UTC. See [`docs/releases.md`](docs/releases.md).
> The checklist below is what it automates — and what to follow if you ever cut
> one by hand.
>
> **Two commands cover the local half.** `npm run release:status`
> prints, for every component, what shipped last, what changed since, whether
> that change can affect a build, and what the next version would be — so step 0
> is never guesswork. `npm run release:prepare -- <comp> [--channel=nightly]`
> then computes the version, refuses it if the component has nothing to ship or
> the base would not move past every channel, writes it into **every**
> version-bearing file, reads them all back to prove they agree, and prints the
> exact commit/tag commands. It never commits, tags or pushes. See
> [`scripts/release/README.md`](scripts/release/README.md).

Cutting a release for component `<comp>` (tag `<comp>-v<version>`; Desktop uses
the channel-specific forms above):

1. **Pre-flight** — the commit you will tag is green on CI (`ci-*.yml`) and its
   `CHANGELOG.md [Unreleased]` accurately describes what ships. `npm run
   release:status` confirms the component genuinely has something to ship.
2. **Mobile only — bump the source version FIRST (non-negotiable)** — set
   `uxnanmobile/pubspec.yaml` `version:` to the release `<name>+<build>` (e.g.
   `0.0.1-alpha.20260621+5`), then **commit and push it**, so the *tagged commit*
   carries the matching version and the Flutter source never lags a tag.
   `release-mobile.yml` **fails the release** if pubspec and the tag disagree.
3. **Mobile only — refresh the Play "What's new" notes** — rewrite
   `.github/whatsnew/whatsnew-en-US` and `whatsnew-es-ES` as a short,
   **non-technical**, user-facing summary of this version's CHANGELOG,
   **≤ 500 characters each**. The release workflow validates both (missing / empty /
   placeholder / over-limit → the release fails). Commit + push before tagging.
4. **Tag & push** — `git tag <comp>-v<version> && git push origin <comp>-v<version>`,
   which triggers `release-<comp>.yml`.
5. **Validate the deploy** — wait for the `release-*.yml` run to go **green** and
   confirm the artifact actually landed: npm shows the new version on the `latest`
   dist-tag (`npm view <pkg> dist-tags.latest`) / the Play **open-testing** (beta)
   track has the new build / the desktop **GitHub Release** draft exists. A red or
   half-finished run is **not** a release — fix it.
6. **Record it** — add the row to the *History* table below (date + the component's
   new version) and commit it to `main`, as the last release step (see the
   automation note under the table).

## Fixing an already-published package's `latest`

The workflow **used to** publish under the `alpha` dist-tag. Because npm only sets
`latest` on a package's **first** publish and `--tag alpha` never moves it, the
already-published packages have `latest` stuck at their first version
(`0.0.1-alpha.20260627`) even though newer versions exist under `alpha`. So
`npm install -g uxnan-bridge` installs the *oldest* build.

The workflow is now fixed (publishes to `latest`), but the **existing** packages
need a one-time manual `latest` move — this requires npm publish rights and is
**not** something CI does. From an `npm login`'d shell, point `latest` at the
newest published version (see the *History* table for the current newest):

```bash
npm dist-tag add @uxnan/shared@0.0.3-alpha.20260702 latest
npm dist-tag add uxnan-bridge@0.0.3-alpha.20260702 latest
# relay's latest is already its newest (0.0.1-alpha.20260627) — nothing to do.
```

Verify with `npm view <pkg> dist-tags`. Optionally drop the now-redundant `alpha`
tag (`npm dist-tag rm uxnan-bridge alpha`) — leaving it is harmless. From the next
release onward the workflow keeps `latest` current automatically.

## History

| Date (YYYY-MM-DD) | shared | bridge | relay | desktop | mobile |
| ----------------- | ------ | ------ | ----- | ------- | ------ |
| 2026-08-07 | — | — | — | 0.0.31-nightly.20260807.3 | — | <!-- fix(release): three defects the first real cut exposed; build(release): desktop 0.0.30-nightly.20260807.2; feat(svelte): make the left panel legible without expanding it; feat: workspace lifecycle — know when a space is finished, and close it; @uxnan-releases[bot] made their first contribution in https://github.com/luisgamas/uxnan/pull/159 -->
| 2026-08-07 | — | — | — | 0.0.30-nightly.20260807.2 | — | <!-- desktop: nightly — search inside files, glob filters and a tree that follows the open file (PR #156); the first cut made entirely by `release.yml`, which exposed three defects fixed in PR #160 -->
| 2026-08-07 | — | — | — | 0.0.29-nightly.20260807.1 | — | <!-- desktop: nightly — dialogs, menus and popovers now open in front of the integrated browser, and an agent tab survives its TUI quitting (PR #150); every catalog agent gets a mark that survives its theme — `pi` gains a favicon domain, Zero and OpenClaude point at their own subdomains, and Codex's `currentColor` mark is inverted on dark themes (PR #152). Also the first release cut with `npm run release:prepare`, which caught two bugs in itself before the tag: it reformatted `tauri.conf.json` while bumping it (PR #157), and `ci-node.yml` did not watch `scripts/`, so the tooling's own tests never ran in CI. -->
| 2026-08-05 | — | 0.0.18-alpha.20260805 | — | — | — | <!-- bridge only, a same-day correction of 0.0.17. **Both usage fixes in 0.0.17 were validated against a surface the bridge does not drive, so neither did anything on a real device** — the maintainer installed it, ran Zero/Grok/Codex and saw nothing. Corrected against the surface it does drive, this time verified END TO END by running the adapters themselves and reading what they emit. **Grok:** 0.0.17 accepted `_x.ai/session/update`, but the update carrying usage arrives on **`_x.ai/session_notification`** — a third method, captured off the live wire while the adapter drove a real turn on the production path (`grok agent stdio`, not the `acp` subcommand). Verified: `turn_completed` emits `{tokens:24381, contextWindow:500000}`. **Zero:** 0.0.17 flipped `reportsContextUsage` to `true` after reading a `provider_usage` event from Zero's session store. That event is real and is written **only for a `zero exec` session**; running the adapter and reading the store it wrote shows an ACP-driven session holds `message` events and nothing else. The phone was therefore showing a context meter pinned at zero — worse than hiding it. Capability honest again (`false`), and `readZeroUsage`/`zeroSessionsRoot` + their exports and tests deleted rather than left unwired. **Codex was genuinely fixed in 0.0.17** and is re-verified here (`{tokens:25235, contextWindow:258400}`); the earlier in-session reading that it also failed came from a stale `dist/`, not from the code. Antigravity stays `false` (its usage exists only under `--output-format stream-json` while the turn runs on `text`). **The docs exist so this cannot recur.** `bridge/docs/agents.md` gains a **Drive surface** table — per agent: which headless surface the bridge drives, its transport/framing, and whether usage is reported there — with the two CLIs that report usage on a *different* surface called out, since that is the trap; it also records that every model list is discovered live except **Claude Code's**, the single curated hand-maintained one. A stale claim was corrected while there (the table said Grok is driven with `grok acp`). **And made discoverable, because nothing routed anyone there:** the only link from `AGENTS.md` sat inside the model-tables paragraph, and its *read first* table sends `bridge/` work to `architecture/02a` §5.8 — which is the hole both wrong fixes fell through. `AGENTS.md` now has a `bridge/src/adapters/` row pointing at *Drive surface*, the rule itself written beside the counts/model-table rules with both real failures named, and `bridge/FOR-DEV.md`'s add-an-agent recipe gains a step 0 (pick the surface, document it in the same change set). Mobile and desktop deliberately NOT re-released: no code of theirs changed — Zero's meter disappearing on the phone is the bridge telling it the truth through the existing capability. `shared/` unchanged, so the pin stays `@uxnan/shared@0.0.13-alpha.20260804`. Bridge 632 passing. From PR #149 (admin-merged, CI green). **Verified:** `uxnan-bridge` resolves 0.0.18-alpha.20260805 on npm's `latest` and its published metadata carries the expected shared pin. -->
| 2026-08-05 | — | 0.0.17-alpha.20260805 | — | 0.0.28 | 0.0.18-alpha.20260805+20260805 | <!-- three components cut together; `shared/` deliberately NOT released — nothing under it changed, so the bridge keeps pinning `@uxnan/shared@0.0.13-alpha.20260804`, already npm's `latest` (verified on the published package), which is why the tag-ordering rule had nothing to order this cycle. From PR #148 (admin-merged, CI green on all three legs). **The theme is that every fix here was settled against the real CLIs rather than reasoned about, and three of them contradicted a comment the code had been trusting.** **Token usage — three agents had no context meter and no profile metrics, all fixed at the source.** Codex: usage is **not** on `turn/completed` — probed against a live `codex app-server`, a completed turn carries only `{id, items, itemsView, status, error, startedAt, completedAt, durationMs}`, so the adapter had been reading a field that never existed; it arrives on `thread/tokenUsage/updated`, which also carries `modelContextWindow` (so the window no longer depends on a `models_cache.json` lookup that could miss the model). Zero: ACP genuinely carries none, but Zero appends a `provider_usage` event per turn to its own store (`<XDG_DATA_HOME|~/.local/share>/zero/sessions/<id>/events.jsonl`), read live off a real run. Grok: its `turn_completed` rides on **`_x.ai/session/update`**, a second method the adapter dropped whole, and its capability said `false` behind a FOR-DEV that a funded account has now disproved. In all three the cached-input count is a SUBSET of the input count and is never added. Both the phone's context meter and the profile metrics feed from the same `usage`, so both recover together. **Antigravity is the one left open, with its shape recorded** (`bridge/FOR-DEV.md`): it DOES report `{input_tokens, output_tokens, thinking_tokens, cache_read_tokens, total_tokens}`, but only under `--output-format stream-json` while the turn runs on `text` — migrating the whole stream parse was too big to bolt on beside three one-line reads. Also verified while there: its reasoning effort is not missing, it is encoded in the model id (`gemini-3.6-flash-low|medium|high`). **Conversation naming reached the five agents it had been silently skipping.** Antigravity never reached `done` because its hook payload names no event at all (`invocationNum`/`fullyIdle`/`terminationReason`), so reports arrived unidentifiable and were discarded — the event name now travels as the reporter's second argument and arrives as `X-Uxnan-Event`. Grok dispatches its hook events in snake_case (`hookEventName:"stop"`) though it accepts PascalCase in config, so every Grok report was dropped and its card sat on "working" forever. Naming is now read from the session's **terminal transcript** (the only source every agent has: measured across a real run of all seven, only Claude reports a prompt/reply through the hook), survives one transient failure instead of being one attempt per session ever, and logs every outcome under `[convtitle]` — previously a session that went unnamed left no evidence anywhere. **Streaming replies feel fast again on mobile** — and the first attempt at this was wrong in a way worth recording: a one-frame coalesce window barely helped, because agents emit a delta roughly every 20 ms so almost nothing fell inside 16 ms. Measured on a realistic 6000-char reply arriving as 1500 deltas: 18.9 s of rebuild work per delta, 16.0 s at one frame, 3.4 s at 100 ms. The window now grows with the reply (16→100 ms), putting the throttle exactly where the quadratic cost is; nothing is dropped and nothing waits for the turn to end. **Reply previews** now show the agent's answer for Claude, Antigravity and Grok, each read from the transcript that agent hands us; every other agent keeps showing its **status**, which is the honest fallback and the only one that scales to an unwired CLI. An earlier attempt scraped the terminal buffer, regressed Claude and showed banner text everywhere else; it was reverted and its extractor deleted rather than left unwired. Mobile also: the thread row shows the agent's **latest** reply rather than its first (a repository doc promised "most recent first" while the implementation returned ascending), the queued bubble's dashes are the bubble's **own border** so they cannot drift from its shape, and the reasoning-effort menu no longer opens behind the keyboard. Counts re-derived on the released tree: bridge 635, mobile 839, desktop 501 Rust + 772 Vitest. Base 0.0.28 > every shipped desktop build (stable 0.0.27, nightly 0.0.22). Mobile build 20260805 > last 20260804. **Verified:** `uxnan-bridge` resolves 0.0.17-alpha.20260805 on npm's `latest` and its published metadata pins `@uxnan/shared@0.0.13-alpha.20260804`; the signed AAB uploaded to Google Play open testing (beta); all four desktop installer targets built (MSI/NSIS, deb/rpm/AppImage, aarch64 + x86_64 DMG, 17 assets) and the Release was **published** (prerelease=false, body = GitHub's generated What's Changed from `desktop-stable-v0.0.27`), rolling the stable updater's `latest.json` to 0.0.28 across all 11 platform keys. **Owed:** the mobile and desktop UI of this cycle shipped without the maintainer's on-device review (they were remote) — the streaming cadence, the queued bubble's border, and the effort menu's keyboard behaviour. -->
| 2026-08-04 | 0.0.13-alpha.20260804 | 0.0.16-alpha.20260804 | — | 0.0.27 | 0.0.17-alpha.20260804+20260804 | <!-- four components cut together: both of this cycle's PRs are cross-component. **A queued follow-up can now reach the agent mid-turn** (PR #145): the queue always waited for the whole turn to end, but the CLIs do not — they take what you type at the next tool boundary, inside the running turn, which is what lets you correct an agent without stopping it. `IAgentAdapter.steerTurn` + `AgentCapabilities.steering` + the `delivered` `TurnStatus` (terminal and *successful*, deliberately not `cancelled`: the message DID reach the agent, so `queue/clear` leaves it alone and the drain path never replays it) + `stream/turn/delivered`. The hand-off is deliberately narrow — adapter advertises steering, a turn in flight, queue empty and not paused — and **every refusal falls back to the queue**, so a message is never lost, only delayed. Verified live on Claude Code (`--input-format stream-json`, prompt and follow-ups on an open stdin — which also means the pipe MUST be closed at every terminal path or the CLI waits forever), OpenCode (`prompt_async` on the busy session) and pi (`--mode rpc` `steer`); Codex (`turn/steer`) is implemented against the published schema but **never run** — the account was at 100% of its weekly limit with 0 credits. Antigravity/Zero/Grok have no such channel and keep waiting; Zero's own TUI behaves that way too. **Conversations get a real name** (PR #146): a thread was labelled with the first ~72 chars of its opening message, so two that start with the same phrase were indistinguishable, and on the desktop the label was the *latest* prompt, so it moved every time the user spoke. **No agent CLI can help** — titling is the client's job in all of them, and the headless surfaces expose none (a thread uxnan creates comes back from `codex thread/list` with `name: null`; a fresh OpenCode session stays `"New session - <timestamp>"`; Claude's session `name` is `nameSource:"derived"`, from the folder). uxnan is the client, so it names its own: provisional from the opening message, then the agent writes the real one once the first turn has an answer, as a one-shot with **no session id** (nothing enters the thread's history) on the agent's **cheapest** model. `Thread.titleSource` (`prompt`|`agent`|`user`) is the conflict rule — generated replaces provisional, **never** the user's hand-rename — and `stream/thread/renamed` converges every client. Wired for all seven agents and verified live on six (Claude `haiku`, Codex `gpt-5.4-mini` via `codex exec --ephemeral -s read-only -o <file>`, OpenCode, pi, Antigravity `gemini-3.6-flash-low`, Grok); Zero's `zero exec` form is confirmed against Zero's own source but never run (uninstalled, no credits). Desktop has its own path (`convtitle.rs` reusing the AI-commit `run_headless`), on the card's first line and on the tab **below `customTitle`**, so a hand rename still wins. **Model ids are checked against each account's real list, never assumed** — that caught a live bug before it shipped: the desktop mapped `agy` to `gemini-2.0-flash`, which Antigravity does not offer (`agy models` lists `gemini-3.6-flash-*`), so every Antigravity title would have failed silently. The Codex model was picked on measurement, not intuition: `gpt-5.4-mini` at its default effort used 9,696 tokens for a title vs `gpt-5.6-luna` at `low` with 14,186 — and `minimal` is not offered on this account at all. Merge-time reconciliation: both PRs add a method to the same anchor in the same four adapters, the same manager and the same contract file, so every conflict was two ADDITIONS rather than a disagreement; resolved by keeping both, then repairing four seams a naive concatenation cut through mid-method. Counts re-derived on the released tree: notifications 10 → **12** (one per feature), shared 36, relay 30, bridge 632, mobile 831, desktop 495 Rust + 770 Vitest. Base 0.0.27 > every shipped desktop build (stable 0.0.26, nightly 0.0.22). Mobile build 20260804 > last 20260803. `relay/` deliberately NOT released — the only change under it since `relay-v0.0.2-alpha.20260720` is a FOR-DEV.md preamble line. `web/` carries no tag. From PRs #145, #146 (admin-merged, CI green). **Verified:** both npm packages resolve on the `latest` dist-tag and the bridge pins `@uxnan/shared@0.0.13-alpha.20260804` (the tag-ordering rule honoured: shared was pushed alone and confirmed on npm before the bridge tag, which is exactly what 0.0.14 got wrong last cycle); all four desktop installer targets built (MSI/NSIS, deb/rpm/AppImage, aarch64 + x86_64 DMG) and the Release was **published** (prerelease=false, body = GitHub's generated What's Changed from `desktop-stable-v0.0.26`), rolling `desktop-updater-stable`'s `latest.json` to 0.0.27 across all 11 platform keys; the signed AAB uploaded to Google Play open testing (beta). **Owed:** the mobile and desktop UI of both features shipped without the maintainer's on-device review — the queued-bubble dashed outline, the thread row's second line, and the conversation name on the desktop card and tab strip -->
| 2026-08-03 | 0.0.12-alpha.20260803 | 0.0.15-alpha.20260803 | — | 0.0.26 | 0.0.16-alpha.20260803+20260803 | <!-- four components cut together: this cycle's three merged PRs are all cross-component. **Sleeping agents no longer end their turn early** (PR #141): timed against the real `claude` CLI, it emits its end-of-turn `result` and **keeps running**, and when background work finishes in time it **wakes the model** for a second turn on the same process — the bridge ended the turn at the first `result`, so the phone dropped its "responding" state, the queue drained a follow-up into a busy CLI, and the wake-up turn overwrote the reply the user had already read. `system` lines are no longer all parsed as `init` (that is how the `task_started`/`task_notification` signal was thrown away), completion is held while a task is live, and work the CLI kills (~4–6 s grace, measured) is reported as a warning block instead of passing as a clean success; `ThreadStore` and `AgentManager` refuse a second terminal event **adapter-agnostically**, since every adapter except Antigravity ends its turn on a protocol event while its CLI keeps running. Same PR gives the desktop a **post-mortem record** (rolling `<app-data>/logs/`, panic hook, `window.onerror`/`unhandledrejection` forwarding, unclean-shutdown marker surfaced in Settings → App → Diagnostics) — the black-screen force-close left no Windows event, no WebView2 minidump and no log of our own. **Cross-worktree file links** (PR #142): new `workspace/resolveFileLink { cwd, href }` canonicalizes a path an agent cited and returns the viewer's `cwd + path`, picking the target's own Git root when the file was written in a different worktree; deliberately not confined to `browseRoots` (a paired phone already reads any `cwd` it names), guarded by the same existing-regular-file + `.git`/sensitive-segment rule as every workspace read. Plus **durable context-compaction markers** (Codex, Claude, OpenCode, pi — Zero/Grok ACP and Antigravity expose no trustworthy signal, so none is inferred), **native-session `turn/list` convergence** (completed turns written from Codex/Claude/OpenCode/pi/Zero/Grok clients merge into the same thread, mobile polling every 3 s while idle), **response boundaries** so a terminal event can no longer erase earlier commentary, and **Gemini retired to non-runnable legacy** (`deprecated:true`, unavailable, new turns/models/commands rejected; no mobile product surface at all). **Mobile workspace previews** (PR #140): Markdown as GitHub renders it (alerts, `<details>`, README HTML) with shields typed from the *response* rather than the URL and drawn by `jovial_svg`, animated GIF, SVG Preview/Source/Changes parity, and native PDFium preview — `workspace/readFile` now identifies `.pdf` by extension and preserves its bytes (a PDF prefix without a NUL was being decoded as UTF-8 and corrupted) under its own 20 MiB ceiling. Merge-time reconciliation: #141's catch-all `subtype !== 'init'` branch would have swallowed #142's `compact_boundary`, so the `case 'system'` order was rewritten by hand; `architecture/02a` §5.4.7 disagreed with itself on Mermaid and was resolved against the code (no `flutter_inappwebview` in `pubspec.yaml`; a `FOR-DEV:` renders the fence as source). Counts re-derived on the released tree: shared 36, relay 30, bridge 597, mobile 827, desktop 488 Rust + 744 Vitest. Base 0.0.26 > every shipped desktop build (stable 0.0.25, nightly 0.0.22). Mobile build 20260803 > last 20260729. `relay/` deliberately NOT released — the only change under it since `relay-v0.0.2-alpha.20260720` is a FOR-DEV.md preamble line. `web/` carries no tag. From PRs #140, #141, #142, #143 (admin-merged, CI green). **Verified:** both npm packages resolve on the `latest` dist-tag; all four desktop installer targets built (MSI/NSIS, deb/rpm/AppImage, aarch64 + x86_64 DMG) and the Release was **published** (prerelease=false, body = GitHub's generated What's Changed from `desktop-stable-v0.0.25`), rolling `desktop-updater-stable`'s `latest.json` to 0.0.26 across all 11 platform keys; the signed AAB uploaded to Google Play open testing (beta). **Superseded:** `bridge-v0.0.14-alpha.20260803` published pinning `@uxnan/shared@0.0.11-alpha.20260729` — `release-npm.yml` reads the pin from npm's `latest` at build time and both tags were pushed together, so the bridge job resolved shared before 0.0.12 landed. That shared build knows 68 methods, and `HandlerRouter` validates against `isKnownMethod`, so 0.0.14 would have answered `workspace/resolveFileLink` with "method not found". Re-released as 0.0.15 (no source change, correct pin) and the tag-ordering requirement is now written into *Convention* above -->
| 2026-08-02 | — | — | — | 0.0.25 | — | <!-- desktop: stable — the measurement-and-evidence cycle, plus richer file previews. Everything landed since 0.0.24 is desktop-only: **agent session restore completed** (PR #128) — every tab's session is restored, not just the focused one, and uxnan **names the session itself at launch** for the CLIs that accept a caller-chosen id, so a conversation opened and never used comes back too; **agent logos and install detection fixed** (PR #129, the CSP was blocking the logos) plus `UXNAN_DATA_DIR` to relocate the app data dir; a **reproducible resource benchmark** (PR #130: harness, scenarios, the first approved Windows baseline, ~250 MB private memory documented); **right-panel data stays live** (PR #131); the **test pyramid L0–L5** (PR #132: Svelte component tests over Tauri's own `mockIPC`, Rust integration against real temp dirs/processes, a real **E2E suite over the packaged app**, and a machine-checked quality matrix); an **in-app resource monitor** (PR #133: evidence-based attribution with explicit confidence, a zero-cost parked sampler, consent-first sanitized export, its own R12 overhead scenario); **resource mode** (PR #134: Efficient / Balanced / Performance presets + per-capability overrides governing every background consumer, Balanced pinned to pre-mode behavior by test); a **platform support matrix** (PR #135: six evidence-gated levels per platform×feature, checked by the required suite — Windows announces `smoke`, macOS/Linux `builds`); the **GitHub integration validated against real data** (PR #136: frozen captures of real `gh` output as parser contracts, a hardened `gh` layer, and the **first supervised live run** — 7/7 green against a disposable sandbox, which corrected two modeled refusal texts to the live bytes); and **richer, safer file previews + a provider-usage fix** (PR #139: PDF/animated-GIF/SVG/README-grade Markdown through a typed safe-HTML parser with no `{@html}` surface, cross-platform relative-asset resolution, and Codex's `used_percent` no longer reading 1% as 100% while polling now starts at app start and catches up on focus). Suites re-derived on the tagged tree: **477 Rust** (449 unit + 28 integration, 8 intentionally ignored supervised/host probes) + **726 Vitest** across 64 files. Base 0.0.25 > every shipped desktop build (stable 0.0.24, nightly 0.0.22). Same cycle but deliberately **not** released: `bridge/` and `uxnanmobile/` changed by documentation only (the Gemini-deprecation note; the README download badge) and `shared/`/`relay/` not at all, so no npm or Play build was cut. `web/` carries no tag — the landing's conversion redesign (PR #138) deployed to Cloudflare Pages on merge. From PRs #137, #138, #139 this session (admin-merged, CI green) on top of #128–#136 -->
| 2026-07-29 | 0.0.11-alpha.20260729 | 0.0.13-alpha.20260729 | — | 0.0.24 | 0.0.15-alpha.20260729+20260729 | <!-- all four components cut together, because this cycle's two headline features are cross-component. **Message queue** (PR #125): a `turn/send` arriving with a turn in flight is stored as a `queued` turn and drains on completion instead of clobbering the running one — which is also what finally enforces the one-turn-per-thread the one-shot CLIs require (`AgentManager.sendTurn` never checked); run options frozen at queue time, the queue held after a stop/failure until `queue/resume`/`queue/clear`, `turn/cancel` on a queued turn marking it `cancelled`, capped at 10, orphans closed at startup. The phone mirrors it: a floating "Queue message" action, ghost bubbles with a corner edit/cancel and their place in line, and a *Send them / Discard* banner when the bridge holds the queue — gated on the new `bridge/status.features.messageQueue` so an older bridge is never asked to start a second concurrent turn. **Image attachments** (PR #126): the bridge materializes each attachment into the agent's **own working directory** and references it by relative path (every wired CLI can open a file, none takes inline base64 headlessly, and all of them refuse a path outside their workspace), with adapter-native delivery for a protocol that carries images itself (Zero's ACP image block, `IAgentAdapter.handlesAttachments()`); on the phone the thumbnails ride inside the composer pill before sending and above the bubble once sent, an image-only message dropping the bubble entirely. Desktop (PRs #124, #127): the discontinued **Gemini CLI** is retired from AI commit / PR bodies / the provider picker while **Grok and Antigravity** gain managed status reporters (a shared parameterized `curl` hook, since both are single native binaries with no Node guarantee) and Grok gains the browser MCP tools; GitHub becomes reachable from a **worktree's right-click menu** (the only way in while the sidebar is grouped by status), the right-panel tab becomes a real repo digest whose every row opens that item's detail in-app, and the status-bar GitHub readout moves inside the backend popover. Merge-time reconciliation: both PRs had written a different `architecture/02a` §5.8.12 (the queue moved to §5.8.13 with its cross-references), the merged composer dropped the `hasAttachments` prop #126 replaced with the list, a queued message keeps its bubble even when image-only (the edit/cancel actions live in its corner), and #126's tolerant-decode test moved off `cancelled` — a value this release makes known. Counts re-derived on the merged tree: shared 36, bridge 560, mobile 759, desktop 368 Rust + 369 Vitest. Base 0.0.24 > every shipped desktop build (stable 0.0.23, nightly 0.0.22). Mobile build 20260729 > last 20260724. From PRs #124, #125, #126, #127 (admin-merged, CI green). Verified: both npm packages resolve on the `latest` dist-tag (bridge pins @uxnan/shared 0.0.11-alpha.20260729), the signed AAB uploaded to Google Play open testing (beta), and all four desktop installer targets built (MSI/NSIS, deb/rpm/AppImage, aarch64 + x86_64 DMG) into a **draft** stable Release (prerelease=false) whose body is GitHub's generated What's Changed from `desktop-stable-v0.0.23`; publishing it rolls `desktop-updater-stable`'s `latest.json` — stable channel -->
| 2026-07-26 | — | — | — | 0.0.23 | — | <!-- desktop: stable — first `desktop-stable-v*` since 0.0.18; promotes the whole 0.0.19→0.0.22 nightly line (flexible worktree creation + safe opt-in removal, VSCode-style file-tree UX, the experimental unsigned macOS DMGs, the per-project inline GitHub view + sidebar profile card) to the stable channel, plus this cycle's three PRs. **Automations** (PR #121, spec `02f`): unattended, **recurring** tasks that run in their own working folder driving a multi-agent graph — parallel + fan-in from `dependsOn`, context passing (`{{steps.s1.output}}`, `{{prev.s1.output}}` across runs), completion **verified by exit code**, shell precondition, optional per-run worktree, overlap policy and retention. The binary doubles as a **headless runner** (`--automation-run <id>`, branched in `main.rs` before Tauri builds a window), so a run fires **with the app closed** — one execution path shared by the OS scheduler and "Run now"; saving one registers a task with the **OS's own scheduler** (Task Scheduler XML / LaunchAgent / systemd user timer, per user, no elevation) and degrades honestly when registration fails. Ships with its screen (profile menu + `Mod+Shift+A`), four seeded example automations and a contextual prompt-variable picker; still owed: validating the macOS/Linux registration on real hardware. **Pets** (PR #122): an opt-in animated companion mirroring precise hook state 1:1 (`working`→`running`, `waiting`→`waiting`, `done`→`review`, `blocked`→`failed`, else `idle`) with states that **expire** rather than mirror, rendered by default in its own borderless, transparent, always-on-top desktop window (own per-window capability, `macos-private-api` for the transparent macOS webview); mouse-interactive (watches the cursor via the v2 16-pose look rows, reacts to a poke, rides along when dragged) with an idle personality layer. On-disk format is **the Codex CLI's own**, so community packs load unmodified; importing is a **validating copy** (traversal-checked ids, bare-filename sheet path, ≤24 MiB, image sniff, bounded grid) and uxnan bundles only its own pet (Uxni). Plus the project "+" launcher fix (PR #120): its reset `$effect` tracked `worktreesByRepo`/`activeWorktreePath`, so creating a worktree re-ran it mid-submit and wiped the selection before it launched — now `untrack`ed and snapshotted before the first `await`. Docs reconciled in the merge (counts re-derived on the merged tree, the pets entry's stale per-feature count corrected to 9 Rust + 55 Vitest, `automations/` added to `docs/testing.md`, a README feature bullet for automations). 362 Rust + 340 Vitest. Base 0.0.23 > every shipped build (nightly 0.0.22, stable 0.0.18). From PRs #120, #121, #122 (admin-merged, CI green); all four installer targets built (MSI/NSIS, deb/rpm/AppImage, aarch64 + x86_64 DMG), published as a normal Release (prerelease=false) whose body is GitHub's generated What's Changed only, and `desktop-updater-stable`'s `latest.json` rolled to 0.0.23 across all 11 platform keys — stable channel -->
| 2026-07-24 | — | — | — | 0.0.22-nightly.20260724.1 | — | <!-- desktop: nightly — GitHub stops being a full-screen section and becomes a **per-project inline view** (PR #118): opened from each project card's ⋯ menu → GitHub → Pull Requests / Issues / Actions, it replaces the center + right panels while the left sidebar and the browser panel stay visible, is scoped to that project (no repo selector), and closes the moment any worktree is activated — centralized in `app.openTerminal`/`launchAgent` so no terminal can ever open hidden behind it. Overview is gone; the Account/Session panel + every GitHub preference moved to **Settings → GitHub** (`GithubSettings.svelte`), the status-bar GitHub button became a **passive indicator**, and the `Ctrl/Cmd+G` shortcut + profile-menu entry were removed. The left sidebar's quick actions are now just Search: Settings lives in a **configurable profile card pinned to the sidebar footer** (avatar via the shared IconPicker/EntityIcon, name, description; persisted in `AppSettings.profile`). The Changes panel gains **Check remote** — a read-only `git_fetch` that refreshes ahead/behind so the *behind* count and the Pull button stop waiting on some other fetch. Also folds in the desktop half of PR #119: `agentcli.rs` `CLAUDE_MODELS` is back in sync with the bridge's hand-kept twin (ten models, newest first, adding Opus 5 / Sonnet 5 / Opus 4.5 / Sonnet 4.5), with the two-place-edit rule written into the doc comment, `docs/agent-launch.md` and `AGENTS.md`. 266 Rust (Windows leg; 263 on Linux) + 225 Vitest — no tests added this cycle. Base 0.0.22 > every shipped build (nightly 0.0.21). From PRs #118, #119 (admin-merged, CI green); all four installer targets built (MSI/NSIS, deb/rpm/AppImage, aarch64 + x86_64 DMG), published as a pre-release whose body is GitHub's generated What's Changed only, and `desktop-updater-nightly`'s `latest.json` rolled to 0.0.22 across all 11 platform keys — nightly channel -->
| 2026-07-24 | 0.0.10-alpha.20260724 | 0.0.12-alpha.20260724 | — | — | 0.0.14-alpha.20260724+20260724 | <!-- shared/bridge/mobile: the Claude Code model surface completed (PR #119). bridge: `DEFAULT_DAEMON_CONFIG` now seeds ten concrete Claude versions — adding **Opus 5** plus Opus 4.6/4.5 and Sonnet 4.5 — and `CLAUDE_MODEL_ALIASES` finally advertises the **`fable`** "(latest)" alias that `claude --help` (2.1.x) documents, so the phone's picker offers all four moving aliases and every exact id the CLI accepts; because the seed is a live baseline unioned in at load time, existing `~/.uxnan/daemon-config.json` installs pick it up with no edit. A new test pins the seed to canonical ids only (no date suffixes, no `…[1m]`/`…-fast` routing variants, no bare aliases) and to the newest-first order, and `docs/agents.md` documents the list as a **two-place edit** — its hand-kept twin is the desktop's `agentcli.rs` `CLAUDE_MODELS`, which this cycle also brought back in sync (same ten models, same order); the rule is now in `AGENTS.md` too. mobile: push notifications stop rendering as a blank white square — a dedicated monochrome `ic_stat_uxnan` at five densities plus FCM manifest defaults (icon / color `#1B6EF3` / channel `uxnan_turns`) so the foreground and system-built paths look identical (PR #115) — and the Settings → Models toggle copy names Fable in EN/ES. shared is comment-only (the `AgentModel` doc comments name the fable-aware alias set), so no wire shape, field or validator changed; the bridge release pins @uxnan/shared 0.0.10-alpha.20260724. Verified: both packages resolve on npm's `latest` dist-tag and the signed AAB uploaded to Google Play open testing (beta) by release run 30136477584. Mobile build 20260724 > last 20260723. From PRs #115, #119 (admin-merged, CI green) -->
| 2026-07-22 | — | — | — | — | 0.0.13-alpha.20260722+20260723 | <!-- mobile: conversation lists now retain bridge-provided titles and derive a concise title from the first prompt only while the id placeholder remains, preserving every manual rename; the responding label shimmers before the first token, then disappears while the expressive loader follows streamed text as a trailing cursor; and the no-devices state now renders inside the standard Devices shell with its app-bar actions intact and the bundled Uxnan logo replacing the generic hub icon. Mobile CI passed 702 tests; the signed AAB was uploaded successfully to Google Play open testing (beta) by release run 29964081133. Build 20260723 > last 20260722. From PRs #111, #112, #113 (admin-merged, CI green) -->
| 2026-07-22 | — | — | — | 0.0.21-nightly.20260722.1 | — | <!-- desktop: nightly — EXPERIMENTAL unsigned macOS installer (PR #110): two separate ad-hoc-signed DMGs — Apple Silicon `aarch64` (native) + Intel `x86_64` (cross-compiled on `macos-14`, since GitHub's Intel runners are being retired); `signingIdentity "-"`, `hardenedRuntime false`, `minimumSystemVersion 11.0` — no Apple account, users clear Gatekeeper by hand (`uxnandesktop/docs/install-macos.md`). New `path_env` module enriches the process PATH on a macOS Finder/Dock launch (time-bounded login+interactive shell probe + well-known dirs) so agent/`gh`/`git`/editor detection and PTY shells find Homebrew/npm/version-manager CLIs; seeded terminal profile is a real login shell (`-l`), `zsh` fallback on macOS. CI verifies macOS (Apple Silicon) via `verify-desktop.yml`'s new `os-list` input; the release gate stays `{ubuntu, windows}`; the release matrix builds both mac DMGs with `fail-fast:false`. `latest.json` on `desktop-updater-nightly` now carries `darwin-aarch64` + `darwin-x86_64`. 266 Rust (+5) + 225 Vitest. Base 0.0.21 > every shipped build (nightly 0.0.20). From PR #110 (admin-merged, CI green; a transient Linux AppImage-download flake was re-run) — nightly channel -->
| 2026-07-21 | — | — | — | 0.0.20-nightly.20260721.2 | — | <!-- desktop: nightly — VSCode-style file-tree UX + tab & editor-selection fixes (PR #109): inline file/folder creation (an editable draft row inside the tree, intercalated `folder/file.js` paths via `mkdir -p`, a toolbar "…" New file/folder, and Esc-deselect + empty-area project-root actions), inline rename in the tree (shared `TreeInlineInput`; the old `FileNamePromptDialog` removed), file-tree keyboard shortcuts (F2 rename, Del/Cmd+Backspace → OS trash, Enter/Space open — never while typing), VSCode-style focus (opening a tab no longer steals focus from the tree, so Esc deselects and the tree stays keyboard-operable) with the row highlight tracking the selection rather than "open in a tab", theme-tinted text-shaped editor/diff `::selection` that never hides glyphs (native caret via `caret-color`), and a right-click (or mere focus) no longer switches tabs (right-panel tabs default to `activationMode="manual"`; a center-tab right-click only opens its context menu). Backend `fs_create_file`/`fs_create_dir` accept guarded relative intercalated paths. 261 Rust + 225 Vitest (no tests added by #109). Base 0.0.20 > every shipped build (nightly 0.0.19). From PR #109 (admin-merged, CI green) — nightly channel -->
| 2026-07-21 | 0.0.9-alpha.20260721 | 0.0.11-alpha.20260721 | — | — | 0.0.12-alpha.20260721+20260722 | <!-- shared/bridge/mobile: complete durable activity ledger, LAN-discovery fix, and restore-clone hardening (PR #108). bridge: `~/.uxnan/metrics.json` becomes a v2 global ledger — conversations, turn message/day buckets, reported tokens, connection sessions and mutating Git actions — projected incrementally with an idempotent `threads.json` backfill and five rotating `.bak` generations; deleting a thread no longer subtracts its historical conversations/messages/tokens from `metrics/get`; `metrics/export`+`import` now seal & merge the whole ledger (legacy v1 backups still importable); and the dependency-free mDNS advertiser joins `224.0.0.251:5353` on every eligible IPv4 and sets the outbound multicast interface, so LAN discovery works on multi-homed Windows hosts (a disconnected Ethernet/Tailscale/Hyper-V/WSL route no longer steals it) — still advertising only discovery hints, never the pairing code. mobile: Android Auto Backup + device-transfer now exclude both secure-storage prefs and iOS Keychain uses a this-device-only accessibility class, so a restored install can't clone the Ed25519 identity/notification secret (it re-pairs); the metrics controller stays alive from startup so the full ledger is fetched on every (re)connection. shared: the `metrics/*` backup wire shapes are documented as the complete durable ledger (`imported` counts every advanced row); shapes/method names unchanged, so the bridge release pins @uxnan/shared 0.0.9-alpha.20260721. Mobile build 20260722 > last 20260721. From PR #108 (admin-merged, CI green) -->
| 2026-07-21 | — | — | — | 0.0.19-nightly.20260721.1 | — | <!-- desktop: nightly — flexible worktree creation & safe, opt-in removal (PR #105): a New-branch/Existing-branch toggle with a friendly collision-proof branch-name generator and an optional custom location backed by an in-app folder browser (manual refresh + live change detection); removal is worktree-only by default, with opt-in delete-local (`-d`/force, squash-merge safety net preserved) and delete-remote, and RemoveOutcome reports each branch's fate. Plus configurable per-terminal scrollback (Settings → Terminal, 1k–200k, default raised to 20k) and one-time Windows junction/Redirection-Guard (`os error 448`) failure guidance (PR #98), and in-place pending-action spinners across the async filesystem/Git/GitHub/agent-hook surfaces. 261 Rust + 225 Vitest. Base 0.0.19 > every shipped build (stable 0.0.18). From PRs #98, #105 (admin-merged, CI green) — nightly channel -->
| 2026-07-21 | — | 0.0.10-alpha.20260721 | — | — | 0.0.11-alpha.20260721+20260721 | <!-- bridge/mobile: manual-pairing hardening from real-device reports (PR #107). bridge: `uxnan-bridge start` now bypasses the 24h update-check cache (a bridge that checked while an older build was newest stayed silent for up to a day, so a version-incompatible pair was never told to update); `/pair/resolve` logs accepted/rejected/rate-limited per client IP (never the code) so a Tailscale connect-timeout is no longer misdiagnosed as a bad pairing code; and a refused atomic-write `rename` is retried with a capped backoff (~410ms) so a spurious Windows EPERM/EBUSY/EACCES can no longer strand a turn at `streaming` forever — AgentManager also now fails the turn on a terminal-event throw (defense in depth); suite 535/535. mobile: manual pairing over Tailscale no longer aborts before the tunnel comes up (connectTimeout 5s→20s), and a version-mismatched bridge now says "update the bridge" instead of blaming the pairing code (distinct incompatibleVersion error kind). shared unchanged, so the bridge release pins @uxnan/shared 0.0.8-alpha.20260720. Mobile build 20260721 > last 20260720. From PR #107 (admin-merged, CI green) -->
| 2026-07-20 | 0.0.8-alpha.20260720 | 0.0.9-alpha.20260720 | 0.0.2-alpha.20260720 | — | 0.0.10-alpha.20260720+20260720 | <!-- shared/bridge/relay/mobile: security hardening pass. **Breaking wire change** — the E2EE envelope now binds sessionId/seq/direction as AES-GCM AAD, so replay and reflection are cryptographically enforced instead of resting on an unauthenticated seq counter; SECURE_PROTOCOL_VERSION goes 1 -> 2 and BOTH sides now validate it at the handshake, turning a version-skewed pair from a silent hang (connected, every frame dropped) into a clear 'update both' error. bridge/mobile therefore MUST be installed together. Also: LAN qr_bootstrap enrollment now requires an operator-armed pairing window (any reachable LAN/Tailscale peer could previously self-enroll as trusted), armed by showing the QR/code or by a successful /pair/resolve — the last is what keeps a console-less autostarted daemon pairable, and the window is tied to MAX_PAIRING_AGE_MS so it never expires under the QR it gates; the mobile pairing code is now sent to ONLY the user-chosen host (it was raced against spoofable mDNS candidates, disclosing the code and letting a rogue responder be trusted as the PC); per-IP rate-limiter maps bounded against IP-rotation memory exhaustion (relay + bridge); relay reconnect backs off on accept-then-close. Plus a mobile network-path badge (LAN/Tailscale/Direct/Relay) and path-guard unit tests. Suites: bridge 529, shared 36, relay 30, mobile 702. shared changed, so the bridge and relay releases pin @uxnan/shared 0.0.8-alpha.20260720. Mobile build 20260720 > last 20260719. First relay release since 0.0.1-alpha.20260627. From PRs #99, #100, #101, #102, #103, #104 (admin-merged, CI green) -->
| 2026-07-19 | 0.0.7-alpha.20260719 | 0.0.8-alpha.20260719 | — | — | 0.0.9-alpha.20260719+20260719 | <!-- shared/bridge/mobile: Antigravity (Google's `agy`) wired as the 8th real bridge agent (one-shot per turn, client-owned --conversation UUID + --add-dir, autonomous/plan postures, models via `agy models`) and rendered as a branded choice in the mobile picker while the deprecated Gemini CLI is hidden (wiring kept for easy re-enable); plus a mobile session-recovery fix — a mid-turn app reopen now recovers the FULL agent reply (unconditional turn/list re-seed + resync on every reconnect + authoritative finalize + turn/read reconcile), and beforeText-flagged parallel-subagent blocks no longer split a sentence with an activity card. shared adds `antigravity-cli` AgentId + `ContentBlockParams.beforeText`; bridge also clears `activeTurnId` before a turn's terminal status is observable (fixes a turn/list race). Suites: bridge 493, shared 36, relay 27, mobile 670. shared changed, so the bridge release pins @uxnan/shared 0.0.7-alpha.20260719. Mobile build 20260719 > last 20260718. From PRs #95, #96 (admin-merged, CI green) -->
| 2026-07-19 | — | — | — | 0.0.18 | — | <!-- desktop: stable — agent session resume actually works end-to-end (0.0.17's on-device QA found it dead): the agent:status-changed event now carries the captured session (omitting it silently disabled resume while the backend cache captured fine), the OpenCode plugin forwards the ROOT session's sessionID and the Pi extension forwards explicit session_id/session_file fields, waking a session-less tab clears its leftover launch command instead of starting a fresh agent conversation, and an auto-resumed pane skips the old-screen replay (the TUI redraws its own conversation). Codex capture pinned with a golden test from a real intercepted hook payload (session_id rides every lifecycle event — no agent-specific wiring needed). Resume set: Claude, Codex, OpenCode, Pi. Suites: 257 Rust + 212 Vitest. Base 0.0.18 > every shipped build. From PR #94 (admin-merged, CI green) -->
| 2026-07-18 | — | — | — | 0.0.17 | — | <!-- desktop: stable — workspace session lifecycle: the restored session re-binds to its project at boot (selection/watchers follow with zero clicks; stale workspace keys purged; only the active workspace spawns at startup), workspace sleep/wake (processes stopped, tabs/splits kept; parsed-screen snapshots in an atomic terminal-buffers.json sidecar make scrollback survive restarts), and agent CLI session auto-resume (hook-captured provider ids; claude/codex/opencode/pi TUIs relaunch themselves when they were alive at close). Plus two production bug fixes: the orphaned body pointer-lock that froze all mouse input now self-heals on the next click (and the menu→dialog flows defer opens to prevent it), and the packaged-build file editor renders again (style-src exempted from Tauri's CSP nonce rewriting, which voided 'unsafe-inline' per spec). Suites: 256 Rust + 212 Vitest. Base 0.0.17 > every shipped build (stable 0.0.16, nightly 0.0.15). From PRs #91, #92, #93 (admin-merged, CI green) -->
| 2026-07-18 | — | — | — | 0.0.16 | — | <!-- desktop: first STABLE (`desktop-stable-v*`) since 0.0.9 — promotes the whole 0.0.10→0.0.15 nightly line (multi-agent orchestration + broadcast rework, provider usage incl. Grok/Codex resets, precise agent status + Claude/OpenCode sub-agents, quick commands, `gh`-backed GitHub integration, xterm.js 6 terminal-render fixes) to the stable channel, plus the new 0.0.16 work: "Open with" external editors/IDEs, the window Close-button fix, main-window CSP + http(s)-only in-app browser, hardened local hook/MCP server, bounded always-on backend watchers, and git-mutation characterization tests (backend suite 251). Base 0.0.16 is greater than every shipped nightly (0.0.15), so the updater/MSI accepts it as an upgrade. From PR #90 (`feat/desktop-open-with-panel-ux`, admin-merged) -->
| 2026-07-17 | — | — | — | 0.0.15-nightly.20260717.1 | — | <!-- desktop: terminal rendering — the three root causes fixed by construction: the shared WebGL glyph atlas is never cleared on reveal (it is shared across instances, so clearing it corrupted co-visible panes), xterm instances stay alive and are re-parented on a tab move (destroy+replay and the PTY output ring buffer + `pty_snapshot` are gone), and the spawn/resize race that left the shell on a stale 80x24 grid is removed (`requestPtyResize` is the only path to `pty_resize`; pre-spawn requests are stashed and flushed, a failed resize no longer poisons the dedupe); plus the revealed-pane scroll fix now calls the real xterm 6 API (`core.viewport.syncScrollArea()` — the previous `queueSync()` does not exist in 6.0.0, so optional chaining made it a silent no-op). Also: the `gh`-backed GitHub integration — a full-screen section + right-panel tab, PR review/merge/diff with protection-aware merging (repo settings ∩ base-branch rulesets, auto-merge then admin bypass), issue triage/create, Actions logs, worktree-native `gh pr checkout` / `gh issue develop`, GitHub-flavored Markdown, and optional AI PR-body drafting; no token is stored or read by the app (`gh` owns it). Its write side is implemented but largely unexercised against real GitHub data (PRs #76, #77) — nightly channel -->
| 2026-07-16 | — | 0.0.7-alpha.20260716 | — | — | 0.0.8-alpha.20260716+20260718 | <!-- bridge: an omitted JSON-RPC `params` no longer fails methods whose fields are all optional — the phone omits it when every field is unset, so the default no-passphrase `metrics/export` was answered -32602 (surfaced on the phone as "make sure a PC is connected") and `thread/list` with no projectId silently no-op'd; mobile: profile stats gain a manual refresh + a persisted refresh mode (automatic on open / 5-60 min poll / manual), a rejected export now quotes the bridge's real reason, a Neural Expressive pass over files, Git history, onboarding and the composer, and every remaining spinner adopts the shared M3 Expressive loader (PRs #72, #73, #74). shared unchanged (no source change), so the bridge release pins @uxnan/shared 0.0.6-alpha.20260716. Build 20260718: Play's versionCode must strictly increase and 0.0.7 took 20260717 earlier the same day -->
| 2026-07-16 | — | — | — | — | 0.0.7-alpha.20260716+20260717 | <!-- mobile: fixes the Play in-app update flow — a started update could never be finished and then reported "up to date" forever, stranding a downloaded APK; a check now resumes it from the stage Play reports (PR #71). Build number borrows the next day: 0.0.6 took 20260716 earlier the same day and Play's versionCode must strictly increase -->
| 2026-07-16 | 0.0.6-alpha.20260716 | 0.0.6-alpha.20260716 | — | — | 0.0.6-alpha.20260716+20260716 | <!-- shared/bridge: agent slash commands (agent/commands), provider usage (agent/usageStats), and durable tamper-proof profile metrics (metrics/*) with a per-day per-agent breakdown (byAgentDay); mobile: profile stats + contribution heatmap with an Activity/Tokens lens, encrypted metrics backup, usage & credit, slash-command palette, workspace file search + previews (PR #69) -->
| 2026-07-16 | — | — | — | 0.0.14-nightly.20260716.1 | — | <!-- desktop: terminal render fixes — xterm.js 6.0 upgrade fixes deformed/ghosted text (shared WebGL glyph atlas, #4065/#4480) + reveal viewport re-sync (queueSync) so scroll reaches the true end + slim hover-only scrollbar restored; agent detection stops mislabeling non-agent processes that spawn agent helpers (foreground-job discipline) (PRs #66, #67) — nightly channel -->
| 2026-07-15 | — | — | — | 0.0.13-nightly.20260715.1 | — | <!-- desktop: terminal reliability — blank-pane launch fix (ConPTY startup query race) + ghost/doubled-text fix (xterm addon/core realign) + renderer hardening (PR #65) — nightly channel -->
| 2026-07-14 | — | — | — | 0.0.12-nightly.20260714.1 | — | <!-- desktop: precise agent status (done/waiting fix) + Claude & OpenCode sub-agents + configurable terminal keyboard arbitration + interrupt inference + quick commands + left-panel polish (PR #63) — nightly channel -->
| 2026-07-13 | — | — | — | 0.0.11-nightly.20260713.2 | — | <!-- desktop: richer provider usage — reset time, Codex resets + redeem, account type, Grok $ (PR #62) — nightly channel -->
| 2026-07-13 | — | — | — | 0.0.11-nightly.20260713.1 | — | <!-- desktop: multi-agent orchestration run engine + broadcast rework (PR #61) — nightly channel -->
| 2026-07-11 | — | — | — | 0.0.10-nightly.20260711.1 | — | <!-- desktop: Grok provider usage statistics (PR #60) — nightly channel -->
| 2026-07-11 | 0.0.5-alpha.20260711 | 0.0.5-alpha.20260711 | — | 0.0.9-alpha.20260711 | 0.0.5-alpha.20260711+20260711 | <!-- shared/bridge: interactive ACP question/approval workflows plus Zero and Grok; desktop: file workspace, smart sidebar, agent views and provider usage; mobile: Zero/Grok, question cards and clearer turn errors (PRs #56-#58) -->
| 2026-07-05 | — | — | — | 0.0.8-alpha.20260705 | — | <!-- desktop: tooltip system, project cards/icons/tabs, batch theme import, bulk add projects, worktree gating (PRs #47-#52) -->
| 2026-07-05 | — | — | — | 0.0.7-alpha.20260705 | — | <!-- desktop: update toast redesign with elevated card + release notes link (PR #53) -->
| 2026-07-04 | — | — | — | 0.0.6-alpha.20260704 | — | <!-- desktop: browser MCP server for agents (PR #50) -->
| 2026-07-03 | — | — | — | 0.0.5-alpha.20260703 | — | <!-- desktop-only hotfix: blank-screen (rune in plain .ts) -->
| 2026-07-03 | 0.0.4-alpha.20260703 | 0.0.4-alpha.20260703 | — | 0.0.4-alpha.20260703 | 0.0.4-alpha.20260703+20260703 |
| 2026-07-02 | 0.0.3-alpha.20260702 | 0.0.3-alpha.20260702 | — | 0.0.3-alpha.20260702 | 0.0.3-alpha.20260702+20260702 |
| 2026-06-28 | 0.0.2-alpha.20260628 | 0.0.2-alpha.20260628 | — | 0.0.2-alpha.20260628 | 0.0.2-alpha.20260628+20260629 |
| 2026-06-28 | — | — | — | — | 0.0.1-alpha.20260628+20260628 |
| 2026-06-27 | 0.0.1-alpha.20260627 | 0.0.1-alpha.20260627 | 0.0.1-alpha.20260627 | 0.0.1-alpha.20260627 | 0.0.1-alpha.20260627+20260627 |

> FOR-DEV: once the release pipeline is validated end-to-end, a final step in
> each release workflow can append/update the matching cell here automatically
> (commit the row back to `main`). Until then, add rows by hand at release time.
