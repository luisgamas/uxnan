# Dependencies
node_modules/
.dart_tool/
.packages
build/
target/

# TypeScript / Node build output
dist/
*.tsbuildinfo

# Environment and secrets
.env
.env.*
!.env.example
credentials.json
*.pem
*.key
*.p12
*.jks
# Bridge identity / daemon secrets (never committed; live under ~/.uxnan)
secure-device-state.json
trusted-phones.json
# Firebase / push credentials (FOR-HUMAN assets — never committed)
firebase-service-account.json
google-services.json
GoogleService-Info.plist
*.p8

# CI/CD signing & publishing artifacts — decoded at build time from GitHub
# Secrets on the runner; never commit. (Android keystores are *.jks above.)
*.keystore
key.properties
play-account.json
*-service-account*.json

# Agent CLI local config / test scratch (e.g. bridge Gemini-hook tests write
# a .gemini/settings.json into the cwd — local only, never committed).
.gemini/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Flutter
.flutter-plugins
.flutter-plugins-dependencies
*.iml

# Rust / Tauri
target/
*.rs.bk

# Logs
*.log
logs/
bridge_logs.txt

# OS
.DS_Store
Desktop.ini
