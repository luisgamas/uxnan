Nothing changes on screen in this one. It keeps the app in step with how the PC app now reports each AI provider's usage: when a provider's token lives in the computer's credential store, the PC has to be granted access to it first, and the app understands that answer instead of treating it as an error.
