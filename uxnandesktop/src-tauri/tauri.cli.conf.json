{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeDevCommand": "node scripts/build-cli.mjs && npm run dev",
    "beforeBuildCommand": "npm run build && node scripts/build-cli.mjs"
  },
  "bundle": {
    "externalBin": ["binaries/uxnan-cli"]
  }
}
